jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run tests
        run: npm test
      - name: Deploy to production
        if: ${{ github.event_name == 'push' }}
        run: ./deploy.sh
